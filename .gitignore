# 바이너리·키는 이 리포에 절대 두지 않는다 (docs/11 §0-3, §5.1) — 설치 파일은 GitHub Release asset에만
*.dmg
*.exe
*.msi
*.zip
*.tar.gz
*.app
*.sig
*.key
*.p8
*.pem
*.p12
node_modules/
.DS_Store

# 로컬 환경값(서명·공증 설정) — 공개 리포에 절대 올리지 않는다
.env
.env.*

# 화면 묶음(OTA) 서명은 서빙해야 한다(앱이 검증)
!ota/**/manifest.json.sig
